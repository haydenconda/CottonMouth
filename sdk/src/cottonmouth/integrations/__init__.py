"""First-class integrations between CottonMouth and other tools.

Each integration lives in its own module and is imported lazily, so the core
SDK stays dependency-free. Install the matching extra to use one, e.g.::

    pip install "cottonmouth[litellm]"
"""
