apiVersion: v1
kind: ServiceAccount
metadata:
  name: cottonmouth-backend
  namespace: cottonmouth
  labels:
    app.kubernetes.io/part-of: cottonmouth
  # IRSA: uncomment and set to grant the backend Bedrock access for the
  # Investigate feature. Without it, the core observability demo still works.
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/cottonmouth-bedrock
